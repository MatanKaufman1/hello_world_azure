trigger:
  - main

pool:
  name: matanAgent  

stages:
  - stage: Build
    jobs:
      - job: BuildJob
        steps:
          - script: |
              ls -ltr
              dotnet build  --configuration Release
            displayName: 'Build project'
